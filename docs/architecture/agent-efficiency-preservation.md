# Agent Efficiency Preservation Map

This change reduces repeated routing prose without weakening the repository
contract. Locations use the revised files.

| Obligation                                                                     | Before                           | After                                                                             |
| ------------------------------------------------------------------------------ | -------------------------------- | --------------------------------------------------------------------------------- |
| Request scope, read-only boundary, and unrelated-work protection               | `AGENTS.md` Request Boundary     | `AGENTS.md` Request Boundary                                                      |
| Dedicated worktree, scoped Git, and primary-checkout role                      | `AGENTS.md` Repository Lifecycle | `AGENTS.md` Repository Lifecycle                                                  |
| Local-main or live-preview delivery, `wt:finish`, preview retention            | `AGENTS.md` Repository Lifecycle | `AGENTS.md` Repository Lifecycle                                                  |
| Port 5173 protection and task-owned preview ports                              | `AGENTS.md` Verification         | `AGENTS.md` Verification                                                          |
| Risk-matched checks, browser inspection, seven viewports, and aesthetic review | `AGENTS.md` Verification         | `AGENTS.md` Verification and referenced UI guidance                               |
| Exact-routing table and its read-only matching rule                            | `AGENTS.md` Exact Routing        | `AGENTS.md` Exact Routing                                                         |
| Outbound-review and personal-account protections                               | Personal `AGENTS.md`             | Outbound section preserved byte-for-byte; personal-session authorization retained |
| Native Codex and separate Claude model routing                                 | `.claude/rules/model-routing.md` | `.claude/rules/model-routing.md`                                                  |

The routing rule now names bounded delegation, one final worker result, focused
review, change-based retesting, and natural work boundaries. It removes the
unsupported numeric fan-out claim, the hourly restart instruction, and the
separate-quota claim while retaining the existing delivery contract above.

Personal instructions retain scope and approval boundaries, exact destructive
targets, secret handling, scoped Git and dirty-worktree protection, authorized
local integration, actual-route verification, persistent previews and resource
ownership, honest incomplete delivery, risk-matched testing, Windows elevation,
personal-browser consent, and API migration guidance. Repeated wording was
consolidated and the full-history Astra exception retained. The original is
backed up in Austen's Codex backups directory.

`.codex/config.toml` supplies overridable project defaults: High effort,
120,000-token automatic compaction, 4,000-token stored tool outputs, and
Terra/Medium workers. These do not change the model context-window capacity,
remove skills or integrations, or bypass a permission boundary. The installed
Codex app-server's `config/read` resolved these values successfully. Explicit
task choices still take precedence; global defaults apply outside this project.
